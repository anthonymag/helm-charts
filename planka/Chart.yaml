apiVersion: v2
name: planka
description: Planka, the Trello clone.

type: application
version: 1.1.0

dependencies:
  - name: planka
    version: 1.1.1
    repository: http://plankanban.github.io/planka

